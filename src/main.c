#include "homecookedfunctions.h"
#include <stdio.h>

/**
 * @brief Main function, calls de_and_encryption, taking inputs from CLI
 * 
 * @param argc - argument counter
 * @param argv - input arguments from user CLI
 * @return int - returns 0 for successful execution
 */


int main(int argc, char *argv[])
{
    de_and_encryption(argc, argv);
}
