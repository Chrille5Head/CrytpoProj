{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/crypto",
            "args": [],
            "preLaunchTask": "${defaultBuildTask}",
        }
    ],
    "version": "0.2.0",
}